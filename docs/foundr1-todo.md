# Foundr1 Todo

## Store Timecard

- Calendar add flow: Android currently downloads `.ics` files for shift calendar export. Later review whether to detect Android and offer a Google Calendar event link for single shifts, while keeping `.ics` for batch period export.

